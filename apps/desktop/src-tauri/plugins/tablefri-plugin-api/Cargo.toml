[package]
name = "tablefri-plugin-api"
version = "0.1.0"
edition = "2021"
description = "TableFri 插件开发 API"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lib]
crate-type = ["rlib"]
